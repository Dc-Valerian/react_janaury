import HeroPage from "../HomePage/HeroPage";

const Header = () => {
  return (
    <div>
      Header
      <p>fejjejeir</p>
      <button>ihherre</button>
    </div>
  );
};

export default Header;
